# intent-aware-ui
An interaction paradigm that adapts interfaces to user intent instead of waiting for clicks. Making interfaces easier to interact with through intent-aware assistance. Because Interfaces should understand intent, not just clicks.
